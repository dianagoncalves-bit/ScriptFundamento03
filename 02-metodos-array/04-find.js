const alunos =["Selwyn Kane", "Sukuna", "Gojo", "Luffy", "Roronoa Zoro"];

const aluno = alunos.find((nome) => nome === "Selwyn Kane")

console.log(aluno)
const aluno1 = alunos.find((nome) => nome === "Zoro")

console.log(aluno1)