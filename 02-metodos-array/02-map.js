const numero = [1,2,3,4,5];
 
const dobrado = numero.map((num) =>  num * 2);

console.log(dobrado);

let nomes = ["Selwyn Kane", "Qing Xiag", "Tânatos", "Diana"];

let mensagem = nomes.map((nome) => `olá ${nome.toUpperCase()}`);

console.log(mensagem);